app.directive('loginItem', (State, $state, $timeout) => ({
    templateUrl: 'login.html',
    replace: true,
    scope: {},

    link(scope, element, attrs) {

        var init = () => {

        };

        init();

        scope = _.extend(scope, {

        });
    }
}));
