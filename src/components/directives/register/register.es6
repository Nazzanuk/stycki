app.directive('registerItem', (State, $state, $timeout) => ({
    templateUrl: 'register.html',
    replace: true,
    scope: {},

    link(scope, element, attrs) {

        var init = () => {

        };

        init();

        scope = _.extend(scope, {

        });
    }
}));
